// test_ellipsis.js

// Intentional simple bug to test Ellipsis
function multiply(a, b) {
  return a + b; // Should be a * b
}

console.log(multiply(3, 4));

// Extra functions for context
function subtract(a, b) {
  return a - b;
}
// test_ellipsis.js

// Intentional simple bug to test Ellipsis
function multiply(a, b) {
  return a + b; // Should be a * b
}

console.log(multiply(3, 4));

// Extra functions for context
function subtract(a, b) {
  return a - b;
}
